const letterArray = ["a", "b", "c", "d", "e"];
const arrayReduce = (arr, callback) => {
  const reducedArray = [];
  for (let i = 0; i < reducedArray.length; i++) {
    return reducedArray.push(callback());
  }
};
